package com.springboot.ordermanagement.enums;

public enum OrderStatus {
	// enum - it is an user defined data values
	// this will have constant fixed values
	IN_PROGRESS,
	SHIPPED,
	DELIVERED,
	CANCELLED,
	RETURNED;
}
